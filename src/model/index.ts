/**
 * Public model surface for aggregate builders and DTO projection helpers.
 *
 * Primary entry points: {@link cartToDTO}, {@link createCheckoutAggregate},
 * {@link stockAvailabilityToDTO}, and sellable/price snapshot builders in `./builders`.
 */
export * from "./builders";
