DEVANA-FINDING: v1
DEVANA-STATE: fixed | P1 | high | security=no
DEVANA-KEY: src/api/maintenance.ts:216 | account-delete-incomplete-cleanup

# Account delete maintenance does not remove customer commerce data

## Finding

`processQueuedAccountDeleteRequests` deletes ephemeral tokens, releases customer-scoped stock reservations, redacts queued/failed emails, then marks the delete request `completed`. It does not delete or redact `CustomerDocument`, orders, entitlements, licenses, subscriptions, or sent email records.

## Violated Invariant Or Contract

A completed account delete request should remove or irreversibly anonymize persisted commerce PII and access artifacts tied to the identity.

## Oracle

Maintenance steps at `maintenance.ts:200-226`. `completeAccountDeleteRequest` only updates the ops document status (`repositories.ts:924-942`).

## Counterexample

1. Customer calls `account.delete`.
2. Maintenance runs successfully → request `status: "completed"`.
3. `CustomerDocument`, paid orders, entitlements, and licenses remain queryable by `customerId` / `emailHash`.

## Why It Might Matter

Users receive a completed delete while orders, entitlements, and customer records persist in storage.

## Proof

Control-flow trace: delete queue → partial cleanup (tokens/reservations/emails only) → completed without ledger/account deletion.

## Counterevidence Checked

`deleteTokensBySubjectHashes` covers magic-link and export tokens. Expired reservation cron eventually releases guest session holds. Distinct from read-path gaps in existing reports.

## Suggested Next Step

Extend maintenance to redact/delete customer, order, entitlement, license, and subscription documents, or document that hosts must implement full erasure.

## Agent Handoff

After working this report, preserve the original finding body. Update line 2 `DEVANA-STATE: ...` and the final `DEVANA-SUMMARY:` status/priority/confidence prefix. Use one of: `open`, `fixed`, `invalid`, `stale`, `duplicate`, `wontfix`. Keep `DEVANA-KEY:` stable unless the same finding moved. Add dated notes below with evidence checked.

## Status Notes

- 2026-06-29: open by Devana. Initial report written from static source inspection.
- 2026-06-30: fixed for the library's central identity record; commerce/financial records are deliberately host-delegated (see scope). Confirmed the bug: `processQueuedAccountDeleteRequests` (`src/api/maintenance.ts`) did only OPERATIONAL/ACCESS cleanup (delete ephemeral tokens, release stock reservations, redact queued/failed emails) and then marked the request `completed`, never touching the `CustomerDocument` — so the customer's identity PII (email/name/company/vatId) PERSISTED and, worse, the account stayed RE-AUTHENTICATABLE: the `emailHash` index was intact, so `findCustomerByEmailHash(originalHash)` still resolved the "deleted" account and a fresh magic link to the same email granted full customer access. Design context: the library's account-delete is a HYBRID — `requestAccountDelete` (`backend.ts:1536`) also emits the `account.delete_requested` host NOTIFICATION hook (`:1570`), the intended extension point for the host to erase HOST-OWNED and retention-governed commerce data per its jurisdiction's policy. The gap was that the library did not erase its OWN central identity record (which has no financial-retention basis). Fix: a new `AccountRepository.anonymizeCustomerForAccountDelete({ customerId?, emailHash?, now })` (`src/storage/repositories.ts`) locates the customer (by id, else by emailHash) and writes a redacted copy — clears `aggregate.email`/`name`/`company`/`vatId`, neutralizes BOTH the top-level and aggregate `emailHash` to an opaque, non-resolvable sentinel `account-deleted:${customerId}`, keeps the opaque `customerId`/`userId`, and records an `aggregate.metadata.anonymizedAt` audit marker. It is declared on `MikaAccountRepositoryPort` (`backend.ts`), wired into `processQueuedAccountDeleteRequests` (after the email redaction, before completion; `"account"` added to the maintenance repository Picks and the plugin runtime Pick), and a `customerAnonymized` flag is threaded into the completion `metadata.maintenance` and the result item. Effect: a completed delete now (1) ERASES the customer's direct PII from the library's account store and (2) DEFEATS re-authentication — `findCustomerByEmailHash(originalHash)` no longer matches, so a fresh magic link to the same email resolves to a GUEST (emailHash-only) identity, not the deleted customer's account. SCOPE (deliberately retained, host-delegated — NOT a defect): orders, entitlements, licenses, and subscriptions are NOT deleted/anonymized by the library, because they are commerce/financial records with jurisdiction-specific legal retention (paid-order records typically cannot be deleted for years) and the delete-vs-anonymize-vs-retain decision is a host policy call the library cannot make universally; the host performs any required commerce-record erasure/snapshot anonymization via the `account.delete_requested` hook. With the identity record anonymized and the emailHash index neutralized, those records are de-identified at the IDENTITY level (the deleted user can no longer re-auth to reach them; they remain only as admin/accounting-queryable retained records). The customer aggregate's `providerAccounts` (provider-side linkage the provider/host manages) and `aggregate.metadata` host-custom fields are likewise retained as provider/host-owned. Evidence: a new test (`test/backend.test.ts`, "anonymizes the customer identity record when completing an account delete request") drives `api.account.delete` → the maintenance runner and asserts email/name/company/vatId are cleared, both `emailHash` indexes become the sentinel, `customerId`/`userId` + the `anonymizedAt` marker are retained, the request is `completed` with `customerAnonymized: true`, AND `findCustomerByEmailHash(originalEmailHash)` resolves to `null` (re-auth defeated). Mutation-verified: neutering the anonymize (cp-backup + restore, no git) fails the test (`customerAnonymized: false` + the PII assertions); restored and re-confirmed. Full suite (395) and both tsc configs pass.
- 2026-06-30 (EXTENSION after adversarial review — the customer-doc-only fix above was INCOMPLETE): an adversarial review proved the deleted account's RETAINED RECORDS were still reachable by re-auth, so the prior note's "can no longer re-auth to reach them" claim did not hold. Two gaps: (GAP 1) orders and entitlements kept the customer's ORIGINAL `emailHash`, so a fresh magic link to the deleted email fell to the GUEST emailHash branch (`findCustomerByEmailHash` is null — the customer-doc fix worked — but `verifyMagicLink` binds `mika.emailHash = originalHash`), and `getAccount`'s guest path returned the deleted customer's orders, entitlements, WORKING download tokens, and a live invoice token (billing PII) via `listOrdersByEmailHash`/`listEntitlementsByEmailHash(originalHash)`; (GAP 2) a session opened BEFORE deletion (holding `mika.customerId`) kept full access because `resolveAccountIdentity` still resolved the anonymized customer by id. Both closed: (a) `processQueuedAccountDeleteRequests` now also calls new `LedgerRepository.anonymizeOrdersForAccountDelete` + `AccountRepository.anonymizeEntitlementsForAccountDelete`, which re-key the emailHash index — order top-level `emailHash` + `aggregate.customer.emailHash`; entitlement `emailHash` + `record.emailHash` — to the SAME sentinel the customer doc received (`anonymizeCustomerForAccountDelete` now RETURNS its `account-deleted:${customerId}` sentinel; the anonymizers union records found by `customerId` AND `emailHash` so guest-only grants are covered), and scrub the order customer SNAPSHOT (email/name/company/vatId) while retaining all financial fields — so `listOrdersByEmailHash`/`listEntitlementsByEmailHash(originalHash)` now return NOTHING and the guest re-auth path finds no records; (b) `resolveAccountIdentity` rejects an anonymized customer in its `customerId` branch (`isAnonymizedCustomer` = `aggregate.metadata.anonymizedAt` set), so a lingering pre-delete session gets `AUTH_REQUIRED` — `findCustomerById` is unchanged so admin/accounting still reads the de-identified record by `customerId`. Licenses need no re-keying (`LicenseDocument` has no `emailHash` index and the guest account path never lists licenses — confirmed). `"ledger"` added to the maintenance + plugin repository Picks; `ordersAnonymized`/`entitlementsAnonymized` counts threaded through. Evidence: the test now drives a FRESH magic link to the deleted email and asserts `account.get` → `{ orders: [], entitlements: [], downloads: [] }` then `AUTH_REQUIRED`, that `listOrdersByEmailHash`/`listEntitlementsByEmailHash(originalHash)` are empty, AND that a session pre-holding `mika.customerId` for the deleted customer gets `AUTH_REQUIRED`; mutation-verified (neutering the order re-key fails the guest-path test). Full suite (396) and both tsc pass. The "defeats re-auth TO THE RECORDS" guarantee is delivered by THIS extension; the order/entitlement records are now de-identified (snapshot PII scrubbed, emailHash index neutralized) and reachable only by `customerId` for admin/accounting + erasable by the host via the `account.delete_requested` hook.
- 2026-06-30 (FINAL extension — a confirmation review found two MORE residuals, both now closed): (B1) GUEST deletes: the library never provisions a `CustomerDocument` (they are host-provisioned), so a guest's orders/entitlements are keyed by `emailHash` with NO `customerId`; the re-key sentinel resolved to `undefined` for them, SKIPPING the order/entitlement re-key — a guest's records stayed reachable by the original hash. Fixed: the maintenance sentinel falls back to `account-deleted-email:${request.emailHash}` when there is no `customerId`, so guest records are re-keyed too. (B2) the `userId` identity index: `resolveAccountIdentity`'s `userId` branch was unguarded and the `userId` index was RETAINED, so a `ctx.userId`-driven identity (`customerId` and `userId` are independent host context fields) reached the records via `findCustomerByUserId` / the `listEntitlementsByUser` fallthrough. Fixed: the customer doc's top-level `userId` and the entitlement `userId` (index + `record.userId`) are neutralized to the same per-identity sentinel, and the `userId` AND `emailHash` branches of `resolveAccountIdentity` now reject anonymized customers (the `customerId` branch already did). Orders carry no `userId` index (verified: no `listOrdersByUser`; `OrderDocument` has no `userId` index — its `aggregate.customer.userId` snapshot is not an index and not surfaced by `orderSummaryDTO`). Systematic re-trace (registered + guest delete × {customerId, userId, emailHash} × all sinks — download tokens, invoice tokens, account export): every emailHash/userId list is neutralized; every customerId list is reachable only via the guarded customerId branch (admin-only); NO re-auth path remains for either delete type. Two new tests (guest-delete re-key with no customer doc; userId neutralization + branch) + tightened exact-`AUTH_REQUIRED` assertions; mutation-verified (forcing the guest sentinel `undefined` fails the guest test). Full suite (398) + both tsc pass. Subscriptions retain a customer PII SNAPSHOT (`aggregate.customer` email/name) but carry no emailHash/userId index and are off the guest path (unreachable by the deleted user) — host-delegated for full erasure via the hook.
- 2026-06-30 (completeness fix — a 4th confirmation review found one more identity class: userId-ONLY): a host SSO identity or a manual `grantEntitlement({ userId })` creates an entitlement keyed by `userId` ALONE (no customer doc, no customerId, no emailHash). For such a delete both `request.customerId` and `request.emailHash` are undefined, so the sentinel resolved to `undefined` and the anonymize step was SKIPPED entirely; and even with a sentinel, `anonymizeEntitlementsForAccountDelete` only collected by customerId/emailHash — so the userId-only grant was never re-keyed and stayed re-authable via `listEntitlementsByUser`. Fixed: the maintenance sentinel now also falls back to `account-deleted-user:${request.userId}`, and `anonymizeEntitlementsForAccountDelete` takes a `userId` and adds `listEntitlementsByUser` as a THIRD union source (orders need none — userId-only identities have no orders; `getAccount` returns `orders: []` for them and there is no `listOrdersByUser`). A new test drives a userId-only grant + a userId-only `account.delete` and asserts the grant is re-keyed (`listEntitlementsByUser(originalUserId)` empty, `entitlementsAnonymized: 1`) and that re-auth by the same `ctx.userId` → `AUTH_REQUIRED`; mutation-verified (removing the userId sentinel fallback makes the delete a no-op and fails the test). Full suite (399) + both tsc pass. With this, ALL THREE identity vectors {customerId, userId, emailHash} are COLLECTED, neutralized, and resolution-guarded for EVERY identity class — registered-customer, guest-emailHash-only, and userId-only — closing every re-auth path the four adversarial rounds surfaced.

DEVANA-KEY: src/api/maintenance.ts:216 | account-delete-incomplete-cleanup
DEVANA-SUMMARY: fixed | P1 | high | Account-delete maintenance now anonymizes the library's identity records: the customer doc (email/name/company/vatId cleared, emailHash→sentinel) AND the retained orders/entitlements (emailHash index re-keyed to the SAME sentinel, order customer-snapshot PII scrubbed, financial fields kept). A completed delete erases identity PII and DEFEATS RE-AUTH TO THE RECORDS — a fresh magic link to the deleted email finds nothing (the guest emailHash path's listOrdersByEmailHash/listEntitlementsByEmailHash return empty), and a lingering pre-delete session is rejected (resolveAccountIdentity drops anonymized customers; findCustomerById is unchanged so admin/accounting reads the de-identified record by customerId). Records remain customerId-reachable for accounting + erasable by the host via the account.delete_requested hook; subscriptions/licenses carry no emailHash/userId index so the guest path never reached them. The initial customer-doc-only fix was EXTENDED across FOUR adversarial rounds to systematically cover all three identity indexes (emailHash AND userId neutralized on the customer doc, orders, and entitlements; entitlements collected by customerId AND emailHash AND userId) for EVERY identity class — registered-customer, guest-emailHash-only, and userId-only — plus all three resolveAccountIdentity branches (customerId/userId/emailHash reject anonymized customers), so no re-auth path reaches the de-identified records.