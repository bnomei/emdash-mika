# Review: U01 / `9928369` — CI `templates:check`

**Commit:** `9928369e9dbb8fc3eb965ca5bc35b1ac052914d0`  
**Checkmark:** P0.4 Ensure PR CI runs `templates:check`  
**Scope:** `.github/workflows/ci.yml` (+1 line)

## Summary

Workflow-only change that inserts `npm run templates:check` into the existing `verify` job between `npm test` and `npm run pack:check`. That is exactly what U01 / P0.4 asked for: PR CI (and `main` pushes) already ran `typecheck` and `types:check`; `templates:check` was the missing gate.

The placement is sound. The script (`astro check --root test/fixtures/astro-template-check`) typechecks templates via path aliases into `src/`, not `dist/`, so it does not depend on `pack:check` / `build:pack`. DevDependencies already include `astro`, `@astrojs/check`, and `@astrojs/react`, so `npm ci` is sufficient. Triggers (`pull_request` + `push` to `main`), concurrency, Node 22, and the 15-minute timeout are unchanged and remain appropriate.

**Verdict:** Accept. Closes P0.4 CI / U01 as specified. Residual note about `prepublishOnly` is out of strict U01 scope but contradicts the commit body.

## Issues

### Issue 1 -- Severity: nit
- File: `.github/workflows/ci.yml:26` (commit message / related gate)
- Description: Commit body says the change makes “CI matches prepublish/local template typechecking,” but `prepublishOnly` still does **not** run `templates:check`:

  `check && typecheck && test && pack:check && types:check`

  After this commit, CI is stricter than publish on the template gate. Local full gate docs (`docs/qa/EXECUTION.md`) do list `templates:check`; prepublish does not. ROADMAP/U01 only required CI; this is not a functional regression of the unit.
- Suggestion: Optional follow-up: add `npm run templates:check` to `prepublishOnly` (same position as CI) so publish cannot skip a gate CI enforces, and/or tighten the commit message wording in future units (“CI now includes local full-gate `templates:check`” rather than “matches prepublish”).
- Status: open
