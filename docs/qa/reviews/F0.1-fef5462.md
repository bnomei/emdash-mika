# Review: F0.1 / `fef5462` — `prepublishOnly` includes `templates:check`

**Commit:** `fef5462e293a4ec965381467c321c34b29b69b2c`  
**Parent:** `82f49129211e8d28246173e7c4123866f9a43d17`  
**Checkmark:** F0.1 `prepublishOnly` includes `templates:check` (closes U01 residual)  
**Scope:** `package.json` (`prepublishOnly` script)  
**Message:**

```text
qa(F0.1): include templates:check in prepublishOnly

Roadmap-followup: F0.1
Verify: templates:check

Align prepublish with CI so publish cannot skip Astro template typecheck.
```

## Summary

Minimal publish-gate fix: insert `npm run templates:check` into `prepublishOnly` after `test` and before `pack:check`, matching CI step order.

| Gate | Order |
| ---- | ----- |
| **CI** (`.github/workflows/ci.yml`) | `check` → `typecheck` → `test` → **`templates:check`** → `pack:check` → `types:check` |
| **`prepublishOnly` (this commit)** | `check` → `typecheck` → `test` → **`templates:check`** → `pack:check` → `types:check` |
| **Before (U01 residual)** | `check` → `typecheck` → `test` → `pack:check` → `types:check` (no templates) |

That is exactly what [ROADMAP-followup F0.1](../ROADMAP-followup.md) and [U01 Issue 1](./U01-9928369.md) asked for: CI already enforced Astro template typecheck; publish could still ship with broken templates. Placement is correct — `templates:check` (`astro check --root test/fixtures/astro-template-check`) typechecks copyable templates via path aliases into `src/`, not `dist/`, so it does not depend on `pack:check` / `build:pack`. The `templates:check` script itself is unchanged and still targets the existing fixture (plain Astro + React; no `emdash()` expansion, as F0.1 forbids).

**Verdict:** Accept. Closes F0.1 / U01 residual as specified. No functional defects found in the change.

## Checkmarks claimed vs evidence

| Expectation | Evidence | Status |
| ----------- | -------- | ------ |
| Insert `templates:check` after `test` (same order as CI) | `package.json` `prepublishOnly` | **Met** |
| `templates:check` script still points at `test/fixtures/astro-template-check` | Unchanged `templates:check` script | **Met** |
| `prepublishOnly` and CI same relative order | CI L23–28 vs `prepublishOnly` | **Met** |
| Do not add `emdash()` to the fixture | Fixture / script untouched | **Met** |
| Stricter publish only (non-breaking for consumers) | Lifecycle script only | **Met** |

## Issues

No issues found.

The change is a single, correctly ordered gate insertion. Residual docs elsewhere (e.g. `docs/qa/EXECUTION.md` §3.4 still describing CI as missing `templates:check` from the pre-U01 era) are **pre-existing** and outside F0.1’s stated file scope (`package.json` only); they do not regress or undermine this commit.

## Non-issues / confirmed correct

- **Order vs pack/types:** Templates use source paths + fixture symlink to `src/templates/astro`; running before `pack:check` and `types:check` is intentional and matches CI / full gate in EXECUTION §3.1.
- **Fixture not packed:** `files` still publishes `dist` + `src/templates` only; `prepublishOnly` runs in the source tree before pack, so `test/fixtures/...` remains available at publish time.
- **DevDependency tools:** `astro` / `@astrojs/check` already required for local/CI `templates:check`; publish machines that skip devDeps and scripts are out of scope for this card.
- **Commit message / verify tag:** Matches card (`Roadmap-followup: F0.1`, `Verify: templates:check`) and accurately states CI alignment (unlike U01’s overstated “matches prepublish” wording).
- **Scope discipline:** No fixture expansion, no workflow churn, no product code — appropriate for a P0-small honesty fix.

## Scorecard

| Dimension | Rating |
| --------- | ------ |
| Checkmark completeness (F0.1) | **Done** |
| CI / prepublish parity | **Strong** |
| Scope control | **Strong** |
| Ready to close F0.1 without follow-up | **Yes** |
